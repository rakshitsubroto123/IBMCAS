@model IEnumerable<IBMCAS.Models.DrugRequest>

@{
    ViewBag.Title = "DrugReuest";
    Layout = "~/Views/Shared/_Layoutchemist.cshtml";
}

<h2>DrugReuest</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RequestPhysicianId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequestDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RequestedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RequestPhysicianId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RequestDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RequestedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DrugRequestId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DrugRequestId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DrugRequestId })
        </td>
    </tr>
}

</table>
